#include <iostream>
using namespace std;
main()
{
    //COMMENTS
    //comments are the part of text written in coding file which is skiped by the compiler, used to tell other what is happening in the line of code
    
    //TYPES OF COMMENTS
    //There is single lined comment which is written by adding doubl slash at the beginning of line or from the starting of the text needs to comment
    
    /* double lined comments are used to write comments on multiple lines if 
    it is required by add forwarding slash / and then staric sign *   to start comment 
    and to close the comment sequence of slash and staric will be reversed like first staric comes *  and
     then later on forwarding slash / comes 
    but both slash and staric must be connected without space*/
    return 0;
}
